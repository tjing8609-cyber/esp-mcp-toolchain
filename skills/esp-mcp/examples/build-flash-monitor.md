# Build Flash Monitor

1. Build with `esp_project_build`.
2. Confirm before `esp_flash_firmware`.
3. Reset with `esp_reset`.
4. Capture serial output with `esp_serial_capture`.
5. Parse errors with `esp_error_parse_log`.

