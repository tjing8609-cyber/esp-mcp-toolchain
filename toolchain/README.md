# Toolchain Package

This folder contains the Python CLI, MCP server entrypoint, and implementation
modules for ESP MCP tools.

Run the CLI from the repository root:

```powershell
python toolchain/cli.py --help
```

Run the MCP stdio entrypoint:

```powershell
python toolchain/mcp_server.py
```

