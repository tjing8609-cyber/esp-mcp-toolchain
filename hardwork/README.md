# Hardwork Context

Store board and hardware context here.

- `raw/`: original schematic, PCB, BOM, datasheets, and serial documents
- `processed/`: concise summaries consumed by tools and resources
- `index/`: machine-readable index and manifest

Raw files should not be overwritten by tools. Processed files may be updated
with source and confidence metadata.

