# Overview

This repository provides a generic ESP toolchain for AI assistants through a
Python CLI and stdio MCP server.

The repository does not contain product firmware. It exposes safe, scoped
operations for ESP development environments.

